@AGENTS.md

# Claude Code note

`AGENTS.md` is the canonical project instruction file. Follow it as the source of truth.
Do not run `/init` to replace project instructions unless the user explicitly asks to regenerate them.
Keep changes surgical: one implementation ticket per session, no opportunistic refactors.
