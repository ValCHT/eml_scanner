---
description: Execute exactly one SOC Email Triage implementation ticket
---

Read `docs/tickets/TICKET-$ARGUMENTS.md` completely and execute exactly that ticket.
Follow `AGENTS.md` and every normative document cited by the ticket.
Check PRECONDITIONS before editing. Modify only FILES ALLOWED and ticket artifacts under `runs/`.
Run every VALIDATION COMMAND. If a prerequisite is missing, return BLOCKED rather than inventing a fallback.
Do not start another ticket, push, merge, deploy, or modify the frozen spec snapshot.
At the end, report files changed, commands/results, artifacts, and DONE/FAIL/BLOCKED.
